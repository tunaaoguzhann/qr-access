package main

import "fmt"

// This binary is just a friendly hint. Run the reference service with:
//   go run ./cmd/service
func main() {
	fmt.Println("Run `go run ./cmd/service` to start the QR reference service.")
}
